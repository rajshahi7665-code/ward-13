# Ward 13 — Prologue

First playable vertical slice for the Ward 13 browser game.

Open `index.html` in Chrome, Edge, Firefox, or another modern browser.

Controls: `WASD` movement, `Shift` run, `E` interact, `Space` attack, `Q` Echo Sight, `F` flashlight.
